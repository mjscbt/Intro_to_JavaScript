/*
 * Programming Quiz: Even or Odd (3-2)
 *
 * Write an if...else statement that prints `even` if the
 * number is even and prints `odd` if the number is odd.
 *
 * Note - make sure to print only the string "even" or the string "odd"
 *
 * Hint: Use the % (modulo) operator to determine if a number is even or odd.
 * The modulo operater takes two numbers and returns the remainder when the
 * first number is divided by the second one:
 *
 */

// change the value of `number` to test your if...else statement
var number = 2;

if (/* your conditional goes here */) {

} else {

}
